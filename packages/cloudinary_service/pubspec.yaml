name: cloudinary_service
description: "Cloudinary service wrapper for Deliverak"
version: 0.0.1

environment:
  sdk: ^3.11.5
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloudinary_flutter: ^1.3.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
