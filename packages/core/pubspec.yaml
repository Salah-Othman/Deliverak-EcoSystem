name: core
description: "Core models, enums, constants, and utilities for Deliverak"
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  intl: ^0.19.0
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.5
  firebase_messaging: ^15.2.5
  firebase_analytics: ^11.4.1
  firebase_crashlytics: ^4.3.1
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  connectivity_plus: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  freezed: ^3.2.5
  json_serializable: ^6.9.5
  build_runner: ^2.4.13
