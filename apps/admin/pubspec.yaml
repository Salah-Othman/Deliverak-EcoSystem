name: admin
description: "Deliverak Admin Panel"
publish_to: 'none'
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.6
  equatable: ^2.0.7
  go_router: ^14.6.2
  firebase_core: ^3.15.2
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.5
  intl: ^0.19.0
  core:
    path: ../../packages/core
  firebase_services:
    path: ../../packages/firebase_services
  local_storage:
    path: ../../packages/local_storage
  repositories:
    path: ../../packages/repositories
  providers:
    path: ../../packages/providers
  ui_kit:
    path: ../../packages/ui_kit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
